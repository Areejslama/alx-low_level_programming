#include <stdio.h>
#include "main.h"
/**
 *  reverse_array-  reverses the content of an array of integers
 *  @a:array to reverse
 *  @n:an integer
 *
 *  Return:elemnts of array
*/
void reverse_array(int *a, int n)
{
	int b;
	int c;
	int d;

	for (b = 0 ; c = n - 1 ; i < c ; i++, c--)
	{
		d = a[i]
		a[i] = a[c];
		a[c] = d;
	}
	
}
