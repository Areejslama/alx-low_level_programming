#include<stdio.h>
/**
 * main-this is entry point
 *
 * Description:program that prints the alphabet
 *
 * Return:0 Always (success)
*/
int main(void)
{
	char ch = 'a';
	/*prints a -Z*/
	while (ch <= 'z')
	{
	putchar(ch);
	ch++;
	}
	char B = 'A';
	/*prints A - Z*/
	while (ci <= 'Z')
	{	
	putchar(ci);
	ci++;
	}	
	putchar('\n');
	return (0);
	
}	
