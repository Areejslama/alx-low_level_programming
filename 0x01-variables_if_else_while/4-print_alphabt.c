#include <stdio.h>
/**
 * main-this is entry point
 *
 * Description: program that prints the alphabet
 *
 * Return :0 Always (success)
*/
int main(void)
{
	char ch = 'a'
	/*prints a-z*/
	while (ch <='a');
	{
		putchar(ch);
		ch++
		grep -v eq
	}
	putchar('\n');
	return (0);
	
}	
