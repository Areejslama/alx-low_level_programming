#include <stdio.h>
/**
 * main-this is entry point
 *
 * Description: program that prints the alphabet
 *
 * Return :0 Always (success)
*/
int main(void)
{
	int ch = 'a'
	while (ch <='z');
	{
		if (ch != e && ch!= q);
		{
		putchar(ch);
		ch++;
		
	}
	putchar('\n');
	return (0);
}	
