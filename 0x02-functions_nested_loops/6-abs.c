#include <stdlib.h>
#include "main.h"
/**
 * _abs -  computes the absolute value of an integer
 * @int:integer must be computes
 *
 * Return:absolute value
*/
int _abs(int)
{
	if ( i >= 0)
	{
	return(i);
	}
	else
	return (-i);
}	
