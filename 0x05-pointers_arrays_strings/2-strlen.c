#include <stdio.h>
#include "main.h"
/**
 * int _strlen- function return the length of string
 * @s:parameter
 *
 * Return:length of string
*/
int _strlen(char *s)
{
	size_t strlen(const char* str);
}
