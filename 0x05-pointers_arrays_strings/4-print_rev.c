#include <stdio.h>
#include "main.h"
/**
 * print_rev- that prints a string in reverse
 * @s:parameter
 *
 * Return:0 Always
*/
void print_rev(char *s)
{
	for (; *s != 0 ; s++)
	{
		_putchar(rev *s);
	}
	_putchar('\n');
}
