#include <stdio.h>
#include "main.h"
/**
 * print_rev- that prints a string in reverse
 * @s:parameter
 *
 * Return:0 Always
*/
void print_rev(char *s)
{
	int a;

	while(s[a] != '\0')
	{	
	      a++;
	}
	for (c -= 1 ; c >= 0 ; c--)
	{
		_putchar(s[c]);
	}
	_putchar('\n');
}
