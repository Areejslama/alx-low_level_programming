#include <stdio.h>
#include "main.h"
/**
 * print_numbers- prints the numbers from 0 to 9
 *
 * Return:printed numbers
*/
void print_numbers(void)
{
	int num = 0;

	if (num <= 57)
	{
	_putchar(num);
	}
	putchar('\n');
}
